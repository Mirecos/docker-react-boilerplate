FROM node:20-alpine

ENV WATCHPACK_POLLING=true
ENV NODE_ENV=$NODE_ENV
ENV REACT_APP_LOCAL_API_BASE_URL="${REACT_APP_LOCAL_API_BASE_URL}"


COPY . ./srv
WORKDIR /srv

RUN npm install
CMD npm run start

EXPOSE 3000