FROM node:20-alpine


ARG NODE_ENV
ARG LOCAL_API_URL

ENV WATCHPACK_POLLING=true
ENV NODE_ENV=$NODE_ENV
ENV LOCAL_API_URL=$LOCAL_API_URL


COPY . ./srv
WORKDIR /srv

RUN npm install

CMD npm run start

EXPOSE 3000